import { StatusBtnPipe } from './status-btn.pipe';

describe('StatusBtnPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusBtnPipe();
    expect(pipe).toBeTruthy();
  });
});
